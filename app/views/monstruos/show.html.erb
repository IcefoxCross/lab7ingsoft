<div class="container">
	<h1><%= @monstruo.nombre %></h1>
	<h3><%= "#{@monstruo.edad} años" %></h3>
	<%= link_to edit_monstruo_path(@monstruo), class: 'btn btn-primary btn-lg', role: 'button' do%>
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar
    <% end %>
</div>
