<nav class="navbar navbar-default navbar-inverse">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to root_path, class: 'navbar-brand' do%>
		<%= image_tag "logo.png", title: "Logo", class: "logo"%> Critter
	  <% end %>
    </div>
	<div class="btn-toolbar">
    <%= link_to monstruos_path, class: 'btn btn-primary btn-lg', role: 'button' do%>
      <span class="glyphicon glyphicon-th-list" aria-hidden="true"></span> Listado de Monstruos
    <% end %>
    <%= link_to new_monstruo_path, class: 'btn btn-primary btn-lg', role: 'button' do%>
      <span class="glyphicon glyphicon-eye-open" aria-hidden="true"></span> Crear Monstruo
    <% end %>
    </div>
  </div>
</nav>